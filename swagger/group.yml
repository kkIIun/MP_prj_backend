definitions:
  schemas:
    Group:
      properties:
        _id:
          type: string
          description: 그룹 id
        groupName:
          type: string
          description: 그룹 이름
        users:
          type: array
          items:
            type: object
            properties:
              _id:
                type: string
              name:
                type: string
          description: 그룹에 참가한 유저의 정보
        created_at:
          type: string
          format: date
          description: 만들어진 날짜
